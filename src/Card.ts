import React from "react"

interface props {
  name:string
  href:string
  body:string
}
const Card : React.FC<props> = ({name,href,body}) => {
  return (
    (
      <div>

    )
    
  )
}

export default Card
